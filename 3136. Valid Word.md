# LeetCode 3136 — Valid Word
[Java Solution for LeetCode 3136 – Valid Word] https://leetcode.com/problems/valid-word/

## 📝 Problem
A word is considered **valid** if it meets **all four** of the following conditions:

1. The word contains **only letters and digits**.  
2. The **length** of the word is **at least 3**.  
3. The word contains **at least one vowel** (`a, e, i, o, u`).  
4. The word contains **at least one consonant** (letters that are not vowels).

Return `true` if the word is valid, otherwise return `false`.

---

## 📌 Examples

### Example 1
**Input:**  
`word = "234Adas"`

**Output:**  
`true`

**Explanation:**  
- Contains only valid characters  
- Length ≥ 3  
- Contains vowels (`a`)  
- Contains consonants (`d`, `s`)  

---

### Example 2
**Input:**  
`word = "b3"`  
**Output:**  
`false`  
**Reason:** Length < 3  

---

### Example 3
**Input:**  
`word = "AeI"`  
**Output:**  
`false`  
**Reason:** No consonants  

---

## 🔒 Constraints
- 1 ≤ word.length ≤ 20  
- Word consists of uppercase/lowercase English letters or digits only  

---

## ✅ Approach  
We check these rules:

1. If any character is **not a digit or letter** → invalid  
2. Count vowels and consonants  
3. Length must be ≥ 3  
4. Must contain ≥1 vowel and ≥1 consonant  

---

## 💡 Java Solution

```java
class Solution {
    public boolean isValid(String word) {
        if (word.length() < 3) return false;

        boolean hasVowel = false;
        boolean hasConsonant = false;

        for (char c : word.toCharArray()) {
            if (!Character.isLetterOrDigit(c)) return false;

            if (Character.isLetter(c)) {
                char ch = Character.toLowerCase(c);
                if ("aeiou".indexOf(ch) != -1) {
                    hasVowel = true;
                } else {
                    hasConsonant = true;
                }
            }
        }

        return hasVowel && hasConsonant;
    }
}
```
⏱️ Complexity
Time Complexity: O(n)

Space Complexity: O(1)

✔️ Summary
A valid word must:
   -  Be at least 3 characters
   -  Contain only letters/digits
   -  Have at least one vowel

Have at least one consonant

This is a common string validation interview question.
