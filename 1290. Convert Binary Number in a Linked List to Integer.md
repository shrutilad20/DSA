# LeetCode 1290 — Convert Binary Number in a Linked List to Integer

## 📝 Problem
You are given the `head` of a singly-linked list where each node contains either **0 or 1**.  
The linked list represents a **binary number**, with the **most significant bit at the head**.

Return the **decimal value** of the binary number.

---

## 📌 Example 1
**Input:**  
`head = [1,0,1]`

**Explanation:**  
Binary `101` = Decimal `5`

**Output:**  
`5`

---

## 📌 Example 2
**Input:**  
`head = [0]`  
**Output:**  
`0`

---

## 🔒 Constraints
- The Linked List is not empty.
- Number of nodes ≤ 30
- Each node's value is either 0 or 1.

---

## ✅ Approach  
As you traverse the linked list, keep building the decimal value:

value = value * 2 + node.val

yaml
Copy code

This treats the number like a left shift.

---

## 💡 Java Solution (Simple & Optimal)

```java
class Solution {
    public int getDecimalValue(ListNode head) {
        int result = 0;
        
        while (head != null) {
            result = result * 2 + head.val;
            head = head.next;
        }
        
        return result;
    }
}
```
⏱️ Complexity
Time Complexity: O(n)

Space Complexity: O(1)

Perfect for constraints: max 30 nodes.

✔️ Summary
The problem is straightforward:
Traverse the list → keep shifting left → build the number → return final decimal value.
